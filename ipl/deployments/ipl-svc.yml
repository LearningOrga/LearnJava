apiVersion: v1
kind: Service
metadata:
  labels:
    app: ipl-service
  name: ipl-service
spec:
  type: LoadBalancer
  ports:
    - name: 9090-9090
      port: 9090
      protocol: TCP
      targetPort: 9090
  selector:
    app: ipl
    #role: backend
  #sessionAffinity: None
